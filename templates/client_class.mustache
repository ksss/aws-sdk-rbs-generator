{{warning_comment}}
module Aws
  module {{service_name}}
    class Client
      def self.new: ({{client_option}}) -> instance
      {{#operations}}
      def {{method_name}}: ({{arguments}}) -> {{returns}}
      {{/operations}}
      {{#waiters?}}
      {{#waiters_first}}
      def wait_until: ({{name}} name, {{params}} params, ?Aws::Waiters::options options) -> {{returns}}
      {{/waiters_first}}
      {{#waiters_others}}
                    | ({{name}} name, {{params}} params, ?Aws::Waiters::options options) -> {{returns}}
      {{/waiters_others}}
      {{/waiters?}}
    end
  end
end

